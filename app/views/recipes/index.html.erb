<div>

  <%= link_to 'New recipe', new_user_recipe_path(user_id: current_user.id) %>

  <% @recipes.each do |recipe| %>  
    
      <% if can? :manage, recipe %>
      
        <%= render recipe %>
      
      <% end %>
      
  <% end %>


</div>